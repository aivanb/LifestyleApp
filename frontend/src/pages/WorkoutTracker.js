import React from 'react';
import WorkoutLoggingDashboard from '../components/WorkoutLoggingDashboard';

/**
 * WorkoutTracker Page
 * 
 * Main interface for workout tracking system.
 * Now uses the new responsive WorkoutLoggingDashboard component.
 */
const WorkoutTracker = () => {
  return <WorkoutLoggingDashboard />;
};

export default WorkoutTracker;